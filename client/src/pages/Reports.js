import React from 'react';
import { Menu, MenuItem } from '@mui/material';
import AppBar from '../components/AppBar'

const Reports = ({setIsAuthenticated}) => {
  return (
    <div>
    
    </div>
  );
};

export default Reports;
